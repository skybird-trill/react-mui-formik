import {
  StyledOverlay,
  StyledProgress,
} from "./style";

const LoadingOverlayComponent = () => (
  <StyledOverlay>
    <StyledProgress />
  </StyledOverlay>
);

export default LoadingOverlayComponent;
